#include <stdio.h>
int main()
{
    int i = 0, even[1000];
    printf ("program that calculates the constitution of a even number in sum of prime numbers");
    printf ("\nenter the wanted numbers, stop condition = 0");
    while (even[i] % 2 == 0)
    {
        scanf ("%d", &even[i]);
        for (int j = 0 ; j <= even[i]; j++)
        {
            
        }
        i++;

    }
}